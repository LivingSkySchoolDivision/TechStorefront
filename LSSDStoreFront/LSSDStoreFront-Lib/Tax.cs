using System;
using System.Collections.Generic;
using System.Text;

namespace LSSD.StoreFront.Lib
{
    public static class Tax
    {
        // These probably shouldn't be hard coded like this but I'm not sure what else to do with them.
        public static decimal GST = (decimal)0.05;
        public static decimal SaskPST = (decimal)0.06;
    }
}
